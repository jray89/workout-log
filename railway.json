{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cd frontend && npm install && npm run build && mkdir -p ../backend/public && cp -r dist/* ../backend/public/ && cd ../backend && bundle install --without development test"
  },
  "deploy": {
    "startCommand": "cd backend && bundle exec rails db:migrate && bundle exec rails server -b 0.0.0.0 -p $PORT",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
