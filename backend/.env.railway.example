# Railway Environment Variables
# Set these in Railway Dashboard → Your Service → Variables

# Rails Environment
RAILS_ENV=production

# Secret Keys (REQUIRED)
# Generate with: cd backend && bundle exec rails secret
SECRET_KEY_BASE=paste_generated_secret_here

# Rails Master Key (REQUIRED)
# Get from: cat backend/config/master.key
# Or generate with: EDITOR=nano bundle exec rails credentials:edit
RAILS_MASTER_KEY=paste_master_key_here

# CORS Origins (REQUIRED)
# Set to your Railway app URL
# Example: https://workout-log.up.railway.app
ALLOWED_ORIGINS=https://your-app.up.railway.app

# Database (AUTO-SET by Railway)
# Railway automatically sets DATABASE_URL when you add PostgreSQL
# DATABASE_URL=postgresql://...

# Optional: Custom Domain Configuration
# APP_HOST=your-app.up.railway.app
# ALLOWED_HOSTS=your-app.up.railway.app

# Optional: Puma Configuration
# WEB_CONCURRENCY=2
# RAILS_MAX_THREADS=5

# Optional: Logging
# RAILS_LOG_LEVEL=info
